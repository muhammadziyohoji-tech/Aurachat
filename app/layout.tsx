import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'AuraChat - Connect Hearts Through Beautiful Conversations',
  description: 'A romantic-themed messenger for private connections and finding your soulmate',
  keywords: ['chat', 'messenger', 'romantic', 'dating', 'connections'],
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}